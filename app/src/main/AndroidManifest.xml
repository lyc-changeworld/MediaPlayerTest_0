<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.achuan.mediaplayer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@drawable/apppicture"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--注册activity和service-->
        <activity android:name=".Video.VideoPlayActivity"></activity>
        <activity android:name=".Video.VideoActivity"></activity>
        <activity android:name=".Music.MusicActivity"></activity>
        <service android:name=".Music.MusicServer"></service>
        <activity android:name=".TabhostActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
